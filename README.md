# homebrew-summon-switcher

Homebrew tap for `summon-switcher`.

Install with:

```sh
brew install liamwh/summon-switcher/summon-switcher
```

The formula in `Formula/summon-switcher.rb` is updated automatically from tagged releases in [`liamwh/summon`](https://github.com/liamwh/summon).
